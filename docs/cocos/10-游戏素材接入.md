# 第一版工业科幻素材

2026-09-24：使用内置 image_gen 生成，无 CLI/API 回退。

资源：`cocos/assets/resources/art/frontier-atlas.png`，实际输出 1254×1254 RGBA，约 1.9 MB。4×4 图集包含 10 种建筑、3 种敌人、地面、铜矿、岩石；煤矿暂用铜矿地表的冷灰色着色版本。

接入：战斗地形/建筑/敌人、建造工具栏、主菜单关卡插图、地图编辑器。`GameArt.ts` 统一加载纹理并按实际尺寸切帧，每格内缩 5 像素去掉生成的边缘杂点。建筑与敌人支持朝向；血条、缺弹提示、货物、范围圈继续使用上层 Graphics。可见精灵节点复用，素材加载失败时回退至原有程序绘制。

验证：TypeScript 检查、67 项 Cocos 项目测试通过；Creator 3.8.8 Web 构建成功，确认资源索引包含图集纹理。尚未完成实际点击试玩和运行画面截图：当前环境没有可用的浏览器控制连接，原生 Chrome 控制未获授权。微信真机和压力场景性能仍需验证。

本地预览：`cd cocos && npm run preview`，打开 `http://127.0.0.1:8090`。

生成提示词（原文；请求尺寸与实际返回尺寸可能不同）：

```text
Use case: stylized-concept
Asset type: production 2D game sprite atlas for a top-down industrial factory tower defense game.
Create ONE square 2048x2048 PNG sprite atlas divided into an EXACT regular 4 by 4 grid of 512x512 cells. No grid lines, no labels, no text. Camera strictly orthographic overhead, no isometric perspective. Crisp hand-painted game sprites with chunky readable silhouettes, beveled gunmetal steel, restrained cyan energy lights and copper/brass mechanical details, subtle ambient occlusion. Designed to read at 32px. Consistent scale and upper-left lighting.
Row 1 left to right: cyan reactor core on square steel foundation; copper mining drill with large circular bit and yellow caution panels; conveyor belt with dark rubber tread and brass rails pointing RIGHT; steel logistics router with central copper hub and four ports.
Row 2: crossing junction with two perpendicular conveyor channels; compact single-barrel cyan gun turret pointing RIGHT; heavy twin-barrel blue armored turret pointing RIGHT; chunky reinforced steel defensive wall.
Row 3: copper-brown industrial storage crate; graphite smelter with orange glowing furnace and blue steel shell; red enemy crawler robot with four legs facing RIGHT; orange fast enemy scout drone facing RIGHT.
Row 4: purple heavy enemy tracked tank facing RIGHT; seamless dark desaturated moss-slate ground tile; copper ore embedded in the SAME ground tile; jagged slate rock outcrop on the SAME ground tile.
Constraints: each of first 13 objects isolated and centered within its own cell, occupies about 85% of cell width, with genuine transparent background around each silhouette. Last three cells are fully opaque square terrain tiles covering their entire exact cell, no border. Exact evenly aligned 4x4 grid, no object crosses its cell. No scenery, no lettering, no watermark, no mockup.
```

