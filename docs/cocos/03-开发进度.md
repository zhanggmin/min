# Cocos 开发进度

更新：2026-09-17。工程：仓库根目录 `cocos/`，Creator 版本锁定为 **3.8.8**（本机也已安装）；规划中的 4.0 文档引用不代表项目版本。

## 2026-09-16 复核结论

当前为「T15 程序闭环已实现，M2 可玩性未通过」，不是首个玩法样本已验收。依据地图和测试，补三格传送带即可让预置防线自动获胜；正式首关未开放钻头建造，却提前要求煤加工与重炮弹药。旧进度末尾直接进入 T16 的安排与 M2 试玩前置条件冲突，现已撤销。

按 [04-T15可玩性纠偏](04-T15可玩性纠偏.md) 执行 T15-R1～R5。此段为纠偏前复核结论；已实现 R1、R2 并收到用户手测通过反馈；R3 已实现累计入库限制及数值对照，待手测，新增记录见文末。保留已有系统及回归测试，优先验证首关自主建造、第二关决策和真人反馈，再进入存档与五关内容。

本次复核执行 `cd cocos && npm test`，44/44 通过；这证明现有规则回归通过，不证明玩法有趣。下文构建、浏览器和性能观测均为此前开发记录，本次未重新进行可视试玩或微信真机验证。

## 当前可用能力

- Boot / Menu / Battle 场景、物流与敌袭实验场、占位图形、地图浏览缩放、批量铺带预览、旋转和拆除。
- 地图校验、核心库存、钻头、带、路由器、交叉器、仓库和石墨工厂；纯 TypeScript 物流守恒测试。
- 普通／重型炮塔、铜／石墨弹药、敌人护甲、空间查询、逻辑子弹和扫掠命中；敌袭实验场已有可持续供弹的普通炮塔。
- 600 建筑、120 移动标记、240 子弹标记的合成显示负载。标记没有战斗逻辑，不能作为完整压力场景验收。

## T11 会话基础

- `GameSession` 成为界面更新入口：命令复制入队，按提交顺序在下一逻辑步执行，随后推进采矿／加工／物流。执行结果包含序号和 tick；失败命令保留原有事务校验。
- 暂停时可提交命令，界面提示排队，继续后执行；前后台切换清空时间积压，恢复后保持暂停。
- 可保存 uint32 状态的种子随机数；会话复制地图，避免调用方修改地图或建造预览改变已提交输入。
- 修复 0.15 秒因浮点误差只推进两步的问题。每步 0.05 秒，单帧最多四步，超额时间丢弃。
- 可选 `waves` 配置与 `WaveScheduler`：敌人类型、出生点索引、数量、延迟和间隔使用整数 tick；达到敌人数上限时，生成回调返回 false，游标保留并在下一步重试。
- 波次时间定义：首波延迟从战斗开始计算，后续波延迟从上波最后一次成功出生后计算；同一波出生间隔从上次成功出生后计算；每步最多出生一个敌人。
- 波次调度器已接入敌人系统；物流地图仍不配置波次。`complete` 仅代表全部出生，不能直接当成胜利条件。
- 补齐 TypeScript ES2016 标准库声明，修复现有 `includes` 类型检查错误。

## 本轮实现：T12 敌人移动与挡路攻击

- `DistanceField`：全体敌人共用反向 Dijkstra 距离场；岩石不可通行，空地代价 1、非核心建筑代价 9。建筑代价为固定破坏惩罚，不按血量实时重算。
- 建筑增删触发刷新，每步最多弹出 256 个堆条目，完成后统一替换距离数组；刷新期间继续使用旧场。初始化成本数组仍是一次地图大小的扫描，256 是扩展预算，不是总耗时保证。刷新期间再次变化会在下一轮重算，避免持续建造使缓存永远不能发布。
- 三类敌人的速度、生命、攻击伤害和攻击间隔集中配置；20 Hz 移动、邻格攻击。当前尚无炮塔、子弹及敌人受伤／护甲结算，后续由 T13 接入。
- 敌人预占来源格和目的格，拥堵时等待或选择下降方向的空邻格；禁止在预占格建造。出生点占用或达到 120 敌人上限时，保留波次游标并延后出生。
- 建筑新增生命值。敌人摧毁建筑不返还建材，内部物品只销毁记账一次；核心归零立即冻结会话，实验界面允许重开。此为实验流程保护，完整胜负与结算仍属于 T14。
- 地图校验拒绝被不可破坏岩石隔断的波次出生点。
- 新增 `maps/pathfinding.json` 和菜单“敌袭实验：封路与拆墙”入口；展示普通／快速／重甲敌人、建筑血条、核心生命、出生倒计时和敌人数。该实验没有胜利目标，可通过暂停、建墙、拆墙观察寻路行为。

## 本轮实现：T13 炮塔、弹药与子弹命中

- 新增纯逻辑 `CombatSystem`。普通炮塔只接收铜，重型炮塔只接收石墨；有合法目标时才扣除一发弹药并进入整数 tick 冷却。
- 两类炮塔使用独立射程、射速、伤害、穿甲和弹速。重甲敌人增加固定护甲，重型炮塔可完全穿透；伤害最低为 1。
- 每个战斗 tick 重建轻量空间网格，炮塔在范围内按距离和稳定敌人 ID 选取目标。逻辑子弹追踪目标并使用线段扫掠，避免 20 Hz 步长下穿透。
- 敌人死亡只结算一次，并同时释放来源格和目的格预占；同一步后续子弹会忽略已死亡目标，不重复统计命中或击杀。
- `GameSession` 的每步顺序扩展为物流、刷怪、敌人移动／攻击、炮塔／子弹；相同输入在不同渲染帧分割下保持一致。
- 敌袭实验场新增“钻头 → 传送带 → 普通炮塔”的真实供弹线，并开放两类炮塔建造；界面显示子弹、敌人血条、击杀数、缺弹状态和炮塔弹药量。
- 补齐 `tools/build.mjs`、`preview.mjs`、`package-report.mjs`。构建脚本验证最新产物并兼容本机 Creator 成功后退出 36 的行为；预览服务限制在构建目录内。

## 本轮实现：T14 胜负、结算与重开

- `GameSession` 增加稳定的 `playing`、`victory`、`defeat` 结果。只有最终波全部成功出生且存活敌人为零时才胜利；核心生命归零始终优先判负。
- 结果产生后清空未执行命令、暂停时钟并拒绝继续推进，避免重复结算或在结束画面后台继续模拟。
- 敌袭 HUD 显示明确的胜利／失败结果，原暂停按钮切换为“重开”；重开重新载入 Battle，创建干净会话。

## 已实现：T15 第一关程序闭环（玩法待纠偏）

- 原物流实验场升级为正式首关配置 `tutorial-01`：保留核心铜线和煤加工线，新增普通炮塔铜弹线，并把石墨线终点改为重型炮塔。三条线各留一个明确缺口。
- 地图格式增加可选 `waveStart`，可用“送入核心的资源”和“两类炮塔内的实际弹药”声明开战门槛。配置校验拒绝空条件、负数、未知资源、无波次条件和不可用的煤弹药。
- `GameSession` 在每个物流步后检查真实供给状态；首关达到核心铜 5、普通炮弹 3、重炮弹 2 后才启动波次倒计时。新增逻辑均补充中文注释，说明教学门槛与 UI 共用状态的原因。
- 菜单入口改为“开始第一关”，战斗 HUD 在备战期显示三项目标进度，达标时提示敌袭倒计时，战斗期切换为核心生命、波次、敌人数和击杀数，最终显示胜负与重开。
- 首关包含 4 个普通敌人和 3 个快速敌人。正确补线可稳定全歼；开战后拆除两座预置炮塔会导致核心失守，形成可胜可败的完整流程。

## 验证

- `cd cocos && npm test`：44/44 通过（新增开战门槛与完整首关正反流程 4 项；原物流、会话、敌人与寻路测试继续通过）。
- `cd cocos && npm run typecheck`：通过。
- 战斗验证覆盖：无弹不射击、弹药扣除与冷却、普通弹对护甲减伤、重弹穿甲、跨逻辑位置扫掠命中、移动预占释放、同一步死亡幂等和帧分割重放。
- 胜负验证覆盖：最终敌人存活时不提前获胜、清场后冻结，以及核心和末敌同一步死亡时核心死亡优先判负。
- 首关自动验证覆盖：条件未满足时波次游标不推进、三类供给全部满足后启动、正确补线稳定击杀 7 个敌人获胜、拆除两座炮塔后核心归零失败。
- `npm run build:web` 已通过 Creator 3.8.8 命令行完成，日志为 `build Task (web-mobile) Finished`；脚本在核对 `index.html` 已刷新后接受 Creator 退出码 36。
- `npm run preview` 已返回 HTTP 200；Chrome 可视冒烟已进入敌袭实验场，确认供弹建筑、两类炮塔按钮、敌人波次、击杀计数与战斗绘制正常。一次观测中核心仍为 600、击杀 10，逻辑 P95 约 0.80 ms；这只是桌面浏览器短时样本，不是性能验收。
- 最新 Chrome 可视回归已实际点击补齐首关三处缺口：备战 HUD 从 `0/5、0/3、0/2` 更新到达标，随后自动进入两波敌袭，最终显示“战斗胜利”、击杀 7、核心 600 和“重开”。
- Web 开发包报告为 17.52 MiB，其中 source map 约占 13.8 MiB；该数字不是微信主包结果。微信构建、分包归属和真机包体仍需单独测量。

## 阶段状态与下一步

| 范围 | 实际状态 / 下一步 |
| --- | --- |
| T01 | 内容基线已写入方案；素材来源清单与规则验收待补 |
| T02 | 工程、三个场景、类型检查、Web 构建／预览／包体报告脚本均可用；编辑器和浏览器启动已验证 |
| T03–T05 | 已有构建配置和合成压力模式；AppID 配置存在，但账号可用性、微信双端启动、分包和性能预算尚无验收证据 |
| T06–T10 | 已有物流原型与测试；多格建筑、完整交互回归和物流真机验证仍待完成，M1 未验收 |
| T11 | 会话、命令、暂停、随机数、波次游标及敌人出生接入已实现 |
| T12 | 寻路、移动、挡路攻击和实验入口已实现；可视交互、真机拥堵表现和性能仍待验收 |
| T13 | 两类炮塔弹药、目标空间查询、护甲、子弹扫掠与实验界面已实现；微信真机战斗和压力数据待验收 |
| T14 | 核心死亡优先、最终波清场胜利、结算冻结与重开已实现；重复结果不会继续推进会话 |
| T15 | 程序闭环已实现；首关内容与自主建造偏离原设计，M2 可玩性未通过，不能仅作为外部补签项 |
| T15-R1 | 用户反馈自主建造、战斗与重开通过；开战/暂停边界未单独反馈，完整录像和体验门槛仍未验收 |
| T15-R2 | 用户反馈手测全部通过；设备信息、触屏录像未提供，微信双端验收仍独立保留 |
| T15-R3 | 累计入库额度、资源/波次数值表、失败与补救对照已实现；59 项回归通过，revision 2 手测待反馈 |
| T15-R4 | 第二关双布局、最低配失败和断供补救已实现；63 项规则回归通过，手动通关及录像待反馈 |
| T15-R5 | 5 名非开发者试玩尚未进行，M2 未通过 |
| T16–T27 | 存档、五关内容、优化及发布验收尚未实现 |

下一步收集 R3 新规则和 R4 第二关手测反馈，修正后组织 R5；暂缓 T16。M2 以真人行为与关卡决策验收；M0/M1 的微信真机、交互与技术欠项同步列入待办。所有前置门槛通过后再进入 M3，不应把本地原型记录为可发布小游戏，也不应把测试通过记为可玩性通过。

## 2026-09-16 本轮：T15-R1 铜教学首关草案

- **实现**：正式入口加载 `copper-tutorial.json`，独立 ID `copper-onboarding-01`、revision 1。48×48，64 铜起步，仅预置核心；开放钻头、带、普通炮塔、墙，只有铜矿。旧 `logistics.json` 保持 ID、revision、布局与自动开战规则，作为开发区旧集成样例。
- **会话规则**：教学依真实物流状态显示采矿、核心送达、供弹和准备就绪。核心收到 1 铜、炮塔拥有 1 铜弹药后可提交 `startDefense`；命令执行时重新检查，暂停时排队，重复开战被拒绝。首波倒计时 200 tick（10 秒），后续波延迟仍从上波最后一次出生计算。
- **关卡草案**：三波分别为普通 3 个、普通 5 个、快速 6 个；可在准备、战斗或结算重开。胜利显示核心生命、击杀与耗弹，不编造供给失败归因。暂未校准为约 5 分钟体验。
- **前移反馈**：炮位射程预览、入口红圈和指向核心的箭头、炮塔缺弹 `!`、开发区菜单。另修复合成压力模式继承旧波次却清空出生点导致地图校验失败的问题。
- **规则回归**：`npm run typecheck` 通过；`npm test` 49/49 通过。新增 `copper-tutorial.test.cjs` 覆盖教学推进、手动开战、精确倒计时、暂停/FIFO/重复命令、自建通关、重开、自然失败及配置校验；原 44 项继续通过。
- **构建**：Creator 3.8.8 Web 构建日志为 `build Task (web-mobile) Finished`，退出码为已知的 36，新产物已实际加载。预览地址 `http://127.0.0.1:8091`。
- **可视操作**：本轮桌面 Chrome 已通过正式菜单进入空白防线，点击建钻头、拖动并确认两条铜线、预览炮塔射程；实际铜送达后目标逐步更新，手动开战按钮解锁。尚未在本轮浏览器操作中执行完整战斗与重开；未录制完整视频。根据用户要求，后续不由代理运行浏览器验证，改为交付手测步骤、等待用户反馈。
- **真人/微信**：尚未进行非开发者试玩、手机触屏或微信双端验证，M2 未通过。
- **实际投入**：本次单次开发会话，未按工时计时，不以估算工日填报实际耗时。

参考可胜布局：`(16,24)` 钻头朝右，`(17,24)` 至 `(21,24)` 向右带接 `(22,24)` 核心；`(20,22)` 钻头朝右，`(21,22)` 至 `(25,22)` 向右带接 `(26,22)` 普通炮塔。总费用 38 铜，开局余 26 铜用于纠错。自动回归全歼 14 敌。自然失败对照保留核心线，改用 `(24,19)` 钻头直供 `(25,19)` 炮塔，炮位偏离进攻路线，未拆防线也会核心归零。这些坐标仅作开发复现，不作为游戏内落点答案。

R1 当时的已知欠项（R3 最新处理见下文）：尚须比较并选择准备期约束，等待可以继续积累核心铜；选定后更新地图 revision、规格和测试。R2 继续校准触摸面积、方向、取消/修复、镜头和受击提示。第二关及 5 人试玩仍待后续，不把当前单炮可胜配置视作持续决策验收。

## 2026-09-16 后续：用户手测反馈与 T15-R2 修正

- **用户反馈**：自主建造通过；战斗与重开通过；建造完成后无法拆除。未提供具体拆除步骤或设备，不将推测作为已复现根因；开战/暂停边界未单独报告。
- **拆除交互**：代码确认旧流程会在切入拆除时清掉选择，且确认按钮仍写“确认建造”。现在保留已有建筑目标，按钮切换“确认拆除”，目标加红框和叉号；也支持先进入拆除、再点击目标。面板显示坐标、确切退款、被销毁存货数量；取消不执行。空地、核心、暂停排队、战斗结束分别给出明确提示。拆除仍在下一逻辑步执行，不绕过会话冻结规则。
- **R2 操作和反馈**：侧栏按钮调整为至少 44 设计像素高，开战和重开不再仅 30 像素；钻头补输出箭头；受伤建筑依真实伤害 tick 闪红 6 tick，保留生命条；拖图超过 8 设计像素后松手不更改选择；触摸取消清理铺带预览。实际触摸面积及可辨性待手测。
- **自动验证**：`npm run typecheck` 通过，`npm test` 53/53 通过。新增 [build-interaction.test.cjs](../../cocos/tests/build-interaction.test.cjs) 直接执行 GameApp 的实际交互处理函数，使用最小 Cocos 替身，不运行浏览器；覆盖建后拆除、后选目标、取消、退款幂等、暂停排队、核心保护、空地、拖图和结算拒绝。它不替代触控命中与渲染验收。
- **手测方式**：按用户要求本轮未使用浏览器工具；下一轮依 [05-R2手动测试](05-R2手动测试.md) 反馈复测。原先自主建造与战斗通过结果保留，不要求重复整套验收。
- **本轮构建**：Creator 3.8.8 于 2026-09-16 17:06 完成 Web 构建，日志 `build Task (web-mobile) Finished`，退出码 36；本地预览服务 HTTP 200。刷新 `http://127.0.0.1:8091` 后可进行新版手测，未由代理验证渲染结果。
- **阶段状态**：R2 修复待用户确认，R3 准备期约束仍未实现；不据单次反馈跳过 R4 第二关、R5 非开发者试玩或微信真机验收。本次单次开发会话，未计量实际工时。


## 2026-09-16 后续：R2 手测通过与 T15-R3 实现

- **用户反馈**：R2 测试全部通过。按用户约定本轮继续不运行浏览器；没有据此补写未提供的设备、录像或 5 人试玩记录。
- **实现**：首关升级 revision 2，准备期核心累计接收最多 24 铜，初始 64 铜另计。消费/退款不重置额度，满额停收并保留线上物品，独立炮塔供弹可继续；实际开战命令执行后解除限制。UI 展示累计入库和核心停收说明，暂停保持原有冻结/命令排队语义。
- **决策与配置**：比较当前库存、累计采矿、首次供弹后限时、累计入库四种方案后选择累计入库。完整数值表、设计理由、参考步骤与手测清单见 [06-R3数值与手测](06-R3数值与手测.md)。基础建设 38 铜、初始预留 26 铜，波次参数保持不变；没有把出生间隔改称清场休整。
- **规则回归**：`npm test` 59/59 通过，类型检查通过。新增 `preparation.test.cjs` 验证长期等待封顶、消费/退款不刷新、并发入库最后额度、暂停、开战解除、旧地图兼容、配置拒绝及真实缓存断供补救。守恒校验包含实际发射弹药消耗。
- **数值证据**：[自动报告](evidence/T15-R3-revision2.json) 记录正常布局 104.25 秒全歼 14 敌且核心 600；偏离道路炮位自然失败；断供不修失败，而核心首次受伤后再延迟 10 秒补线仍获胜、核心剩 432。报告不代替真人体验。生成入口 `node cocos/tools/tutorial-scenarios.cjs`，先运行 `cd cocos && npm test` 编译。
- **可视/触屏**：本轮未自行运行浏览器，待用户验证累计入库说明、停收与开战恢复。约 5 分钟总局长、趣味性仍无非开发者体验证据。
- **后续**：R3 手测反馈后推进 R4 的第二关两套布局；R5 和微信真机验收继续独立追踪。投入为本次单次开发会话，未计量实际工时。


## 2026-09-17：T15-R4 第二关样本

- **实现**：新增 `copper-crossroads-02` revision 1（`crossroads.json`），64×64、44 铜开局、近处两矿与远处两矿、六波 88 敌，仅铜资源；开放路由器和墙，沿用 R3 累计入库 24 铜和手动开战。正式菜单及首关胜利界面可进入第二关，不提前实现存档解锁。
- **界面**：第二关镜头覆盖近远矿、核心和入口；HUD 按地图实际开战条件显示资源，不显示无关石墨目标；预告波次类型与数量，增加真实空弹炮塔和被毁建筑计数。主动拆除不计战损，核心失守仍独立判负。
- **布局证据**：近矿一路分流双炮累计花费 58 铜、400.55 秒胜利；远矿两路独立供前沿双炮累计花费 94 铜（搬迁退款 6）、399.25 秒胜利，均全歼 88 敌、核心 600。最低配 38 铜单炮能过首波，持续不扩建最终失败。
- **补救证据**：一炮断供后等待真实 12 发缓存耗尽，10 秒后补线可恢复该炮射击，最终核心 495；不修对照仍可胜但核心仅 411，证明一次常见失误有恢复空间。五组流程都核对铜守恒，没有灌弹、直接清空炮塔或脚本断供进入正式地图。
- **规则回归**：`npm test` 63/63 通过，`npm run typecheck` 通过。新增 `crossroads.test.cjs` 和被毁计数测试；[自动对照报告](evidence/T15-R4-revision1.json) 可由 `node cocos/tools/crossroads-scenarios.cjs` 重现（先编译测试）。
- **可视/触屏**：根据用户约定不运行浏览器。手动双布局通关、完整录像、实际总时长与决策反馈未采集；参考战斗约 6 分 40 秒，加首次搭建目标 8～12 分钟，尚非真人测量结果。步骤见 [07-R4第二关与手测](07-R4第二关与手测.md)。R3 新规则手测未收到独立通过确认，继续保留待反馈。
- **构建**：2026-09-17 21:52 Creator 3.8.8 最新 Web 构建日志 `build Task (web-mobile) Finished`，退出码为已知的 36；包含当前仓库地图编辑器和新增路由器说明。预览服务已恢复，`http://127.0.0.1:8091` 返回 HTTP 200，未运行浏览器验收。
- **仓库同步**：用户已提交前序工作并新增地图编辑器与注释；本轮在该版本上继续，保留这些变更。地图编辑器不计入 R4 可玩性验收。
- **后续**：先收集 R3/R4 手测并修复，再安排 R5 的 5 名非开发者试玩；M2、微信双端与性能门槛不自动通过。本轮为单次开发会话，未计量工时。
